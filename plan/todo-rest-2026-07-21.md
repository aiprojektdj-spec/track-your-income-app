# Stackr Web 1.7 — Rest-TODO (Stand 2026-07-21)

Löst `plan/todo-rest-2026-07-19.md` ab (Historie/✅-Punkte dort und in
`plan/offene-punkte-2026-07-15.md`). Neu hinzugekommen: Kleinthemen aus der
Differenzbesteuerungs-/EÜR-Planungssession vom 2026-07-21, die keine eigene Session brauchen.
Vor dem Abarbeiten immer `git status`/`git log` frisch prüfen — mehrere Sessions laufen teils
parallel im selben Ordner.

---

## Neu aus der Differenzbesteuerungs-/EÜR-Planungssession (2026-07-21)

Große Themen haben eigene Dateien: `plan/session-prompt-differenzbesteuerung.md` (§25a UStG) und
`plan/session-prompt-euer-umbau.md` (EÜR-Tab nach Rechtsform/Gewinnermittlungsart/Besteuerungsart).
Hier nur die kleinen Restpunkte, die daneben abfallen:

- **Artikelnummer auf Rechnung automatisch übernehmen**: reines UX-Thema (kein Steuerbezug),
  ursprünglicher Telegram-Wunsch des Users. Wenn eine Rechnungsposition über "📦 Artikel aus
  Lager" (`rechnungen/js/rechnung.js`, `addPositionFromLagerArt()`, Zeile ~448) angelegt wird,
  soll die `artikelNr` aus dem Lager-Datensatz automatisch mit übernommen/angezeigt werden. Klein,
  kann unabhängig von der §25a-Arbeit gebaut werden.
- **UVA Kennziffer 500 (2026)**: falls Stackr ein UVA-Voranmeldungsmodul hat/bekommt — bei
  abweichender Bemessungsgrundlage (u.a. §25a-Fälle) wird ab 2026 ein neues Feld/Kennziffer 500
  relevant, das die UVA aus dem automatisierten Verfahren nimmt und zur manuellen Prüfung durch
  das Finanzamt führt. Hinweis/Tooltip im UVA-Modul einbauen, sobald das existiert.
- ~~**euer.js/bilanz.js/gbr-modul.js-Konsolidierung**~~ — erledigt 2026-07-21: `gbr-modul.js` jetzt
  ebenfalls auf `SteuerBerechnung` migriert (Phase 0 der Differenzbesteuerungs-Session), euer.js/
  bilanz.js waren bereits vorher migriert (USt-Bulletproof Punkt 3).
- ~~**Gesamtdifferenz-Methode (§25a Wahlrecht)**~~ — erledigt 2026-07-21: beide Methoden
  (Einzel-/Gesamtdifferenz inkl. Vortrag) ab v1 gebaut, User-Entscheidung (nicht wie ursprünglich
  empfohlen nur Einzeldifferenz).

---

## 🔴 Blockiert auf andere (nicht von einer Coding-Session lösbar)

- **Anwalts-Freigabe §11 AGB-Haftung + §356 Abs.5 BGB Trial-Klausel** — beauftragt,
  Antwort offen. Briefing liegt in `plan/session-prompt-anwalt-briefing.md`.
- **Whop-DPA/AV-Vertrag** — noch nicht angefordert. Recherche+Anfrage-Entwurf:
  `plan/session-prompt-whop-dpa-anfrage.md` (Versand macht User selbst).
- **Echte Live-Tests mit echtem Whop-Login** (User selbst, App ist Login-gated):
  - 2-Tabs-Test Rechnungsnummern-Race-Fix (Punkt 2 USt-Bulletproof): zwei Tabs
    "Neue Rechnung", kurz hintereinander speichern, Nummern dürfen nicht kollidieren.
  - Blob-Sync Live-Test (Upload/Chunking/Content-Hash-Cache/Art.17-Löschung gegen
    echten Vercel-Blob-Store).
  - Kz.41/21-EU-B2B-Test (§13b, pro Position) — Schritte stehen in
    `plan/session-prompt-ust-bulletproof.md` Zeilen 30-39.

## 🟠 Infra/Deploy (nur User, Vercel-Zugriff nötig)

- (aktuell keine offenen Punkte — `CRON_SECRET` und `og-image.png` waren zum 2026-07-19 bereits
  erledigt, siehe `plan/todo-rest-2026-07-19.md` für Details)

## 🟢 Kunden-Feature-Batch Lager-Modul (P1, neu 2026-07-23)

- **Lager-Modul Feature-Batch + USt-ID-Bug GbR** (`plan/session-prompt-lager-feature-batch.md`):
  10 Punkte aus Kunden-Feedback per Q&A geklärt — freie Kategorien + Zielgruppe-Feld, freies
  Status-System, Mehrfach-Farben, editierbare Artikelnummer + eigener Filter, neues
  Lieferant/Händler-Feld, Storno-Freigabe auch bei gesperrten Belegen (GoBD-Check nötig),
  Anmerkungen-Feld am Artikel, globale Klick-Suche, Rechnungs-Lagerdialog mit Filtern+Bild,
  USt-ID-Diagnose. Reihenfolge-Empfehlung und Akzeptanzkriterien in der Datei.

## 🟡 Launch-nah (P1)

- **SEO-Rest** (`plan/session-prompt-landing-seo.md`): echte Lighthouse-Baseline messen (braucht
  DevTools, nicht per MCP messbar), Screenreader-Formulare + Touch-Targets in der App selbst
  (nicht nur Landing) prüfen.
- **Launch-Baseline messen** (Wachstumsplan Juli Woche 1) — noch nicht begonnen.
- **UI-Politur** — eigene interaktive Session mit User, Prompt:
  `plan/session-prompt-ui-politur.md`.
- Weitere Test-Kunden akquirieren — macht User selbst.

## 🔵 Nach Launch (P2)

- **Local 1.7 spiegeln — Schritt 3** (`plan/session-prompt-local-spiegeln.md`):
  Git-Reparatur + Aufräumen ist erledigt, der eigentliche Abgleich Web→Local
  (USt-Regelbesteuerung, GoBD Edit/Delete, Whop-Grace-Token, Datum-Handling, künftig auch
  Differenzbesteuerung + EÜR-Umbau sobald gebaut) steht noch aus — braucht eigene Session.
- **Performance-Rest** (`plan/session-prompt-performance-a11y.md`): Lighthouse-
  Baseline (Score/LCP/CLS/INP, braucht echtes DevTools), Bundle-Splitting-Entscheidung
  für `js/app.js`/`js/store.js` (je >130KB, bisher nur Empfehlung dokumentiert),
  Screenreader-Formulare + Touch-Targets in der App noch nicht geprüft.
- **Whop-Auth Vollaudit** (`plan/session-prompt-whop-audit.md`) — noch nicht als
  eigene Session gefahren: Race-Conditions bei Offline-Grace, Rate-Limit-Lücken,
  Pagination-Limit bei >1000 Memberships.
- `api/whop-token.js` fällt bei fehlender Redis-Env komplett offen (kein Rate-Limit-
  Fallback, nur `console.warn`) — klein, nicht launch-kritisch.
- **USt-Bulletproof Restpunkte** (`plan/session-prompt-ust-bulletproof.md`): Punkt 1
  (§14-Pflichtangaben-Check bei Vorsteuer, Scope-Entscheidung A/B/C nötig), Punkt 2
  (Race-Condition Rechnungsnummern, Web Locks API — nur wenn Aufwand/Nutzen passt). Punkt 3
  (Konsolidierung) ist seit 2026-07-21 komplett erledigt (auch `gbr-modul.js`).

## Fachlich klärungsbedürftig (kein offensichtlicher Fix, beim nächsten USt-Durchgang mitnehmen)

- (aktuell leer)

## Bewusst akzeptierte Lücken (nicht anfassen ohne expliziten Wunsch)

- Superseded Blob-Anhänge werden nicht per Referenzzählung aufgeräumt (kleines
  Kosten-Leck).
- `js/companies.js` `migrateEigenbelegeToCompanies()` schreibt weiter direkt in
  localStorage (nur Alt-Install-Fall, selbstheilend).
- StB-Re-Key beim Zugriffsentzug (Alt-Snapshots bleiben mit altem Schlüssel
  entschlüsselbar).
- Vercel-Blob-Attachments `access:'public'` (durch Verschlüsselung + Random-Suffix
  entschärft).
- Whop-Gate ist für App-Kernfunktionen rein clientseitig (bewusste Local-First-Folge).
- `ui-lab.html` lädt Tabler-Icons ohne SRI (Prototyp, kein Kundendatenzugriff).
- `node_modules/` fehlt in `.gitignore`.

---

**Erledigt seit 2026-07-22**: `plan/session-prompt-euer-nachfixes.md` komplett gebaut — (1)
`Rechtsform.brauchtGewSt()` prüft jetzt `taetigkeitsart`, freiberufliche GbR/EU zahlen 0€ GewSt
(`js/gbr.js` `berechneGewSt()`, `js/gbr-modul.js` `_renderGewerbesteuer()` beide umgestellt); (2)
`Rechtsform.brauchtBilanzStattEuer()` blockt jetzt auch OHG/KG/GmbH & Co. KG sofort (HGB-Kaufmann,
unabhängig von Umsatz/Gewinn), eigener Hinweistext in `js/euer.js`. Logik mit 9 Fällen per
Node/vm-Harness gegen die echten Funktionen verifiziert (Whop-Gate verhindert echten
Browser-Login); Browser-Klicktest durch User weiterhin offen. Local 1.7-Spiegelung noch offen.

**Erledigt seit 2026-07-21**: EÜR-Tab nach §141-AO-Schwelle umgebaut (Rechtsform/Tätigkeitsart-Weiche
statt nur Kapitalgesellschaft-Check) — `js/rechtsform.js`, `js/euer.js`, `js/gbr.js`, `js/gbr-modul.js`,
siehe `plan/session-prompt-euer-umbau.md` Status-Abschnitt. Browser-Klicktest durch User offen
(Whop-Gate, kein Login in Dev-Session möglich).

**Erledigt seit 2026-07-21 (2)**: §25a UStG Differenzbesteuerung komplett gebaut (Plan
`plan/session-prompt-differenzbesteuerung.md`, User-Scope-Entscheidung: beide Marge-Methoden
Einzel-/Gesamtdifferenz + alle 3 Warenarten Gebraucht/Kunst/Sammlerstück ab v1) —
`js/gbr-modul.js` (Phase 0: Migration auf `SteuerBerechnung`, fixt nebenbei 0%-Satz-Bug +
fehlende Rechnungen/Retouren), `js/lager.js`+`lager/page.js` (Phase 1: Datenmodell-Flag+Warenart),
`rechnungen/js/rechnung.js` (Phase 2: kein USt-Ausweis, Pflichttext je Warenart),
`js/steuer-berechnung.js`+`js/store.js` (Phase 3: Marge-Rechenkern+Vortrag-State), `js/euer.js`+
`js/gbr-modul.js` (Phase 4: informativer Ausweis), `js/ustvoranmeldung.js` (Phase 5: Kz.81-Marge,
Kz.500-Hinweis), `js/app.js` (Einzel/Gesamt-Setting). `legal-reviewer`-Agent bestätigt: kein
§14c-Risiko, Kernpfad (Rechnungsausweis) korrekt. Zwei dokumentierte v1-Lücken (beide fehlern
Richtung Überzahlung, kein Compliance-Risiko): pauschal 19% auf Marge (kein 7%-Sonderfall für
Kunst/Sammlerstücke) — UI-Hinweis ergänzt; Retouren auf §25a-Artikel nicht gesondert verrechnet
(vor breiterem Rollout an Händler mit Rückgaben nachziehen). Browser-E2E durch User offen
(Whop-Gate). Local 1.7-Spiegelung noch offen.

**Erledigt seit 2026-07-19** (nur zur Einordnung, volle Details in `todo-rest-2026-07-19.md`):
`.persona-cta`-Touch-Target-Fix (44px, Commit `f04292a`), Eigenbeleg-Vorsteuer pro Beleg statt
pauschal 19% (`euer.js`, Commit `17b311c`).

**Erledigt seit 2026-07-15** (nur zur Einordnung, Details in `offene-punkte-2026-07-15.md`):
USt-Bulletproof Punkt 1-3 (Vorsession-Fixes), Gutschriften §17, Kz.41/21, OSS-Rückwirkung,
`isKlein`-Persistierung, Whop-Gate DevTools-Bypass, CH/AT-Entfernung, Make.com-Webhooks,
Onboarding-Fix, `SteuerBerechnung`-Dedup, `landing-v2.html`/`deploy/`-SEO-Fix, ApexCharts-
Lazy-Load + Skip-Link, Steuerberater-Read-Only (Commit `a76f6d1`).
